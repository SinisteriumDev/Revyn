Plugin Documentation
This will be used to create your custom plugins, otherwise no buttons will show up. Examples of each functions will be provided below the function arguments.

[[ FORMAT ]]
function_name(arguments)
Example

[[ Documentation ]]

CreateSection(nil)
local Section = _G[Plugin Name]:CreateSection()
-- Therefore if your plugin file name is 'Documentation.lua' or 'Documentation.txt' you will use _G[Documentation]


CreateLabel(Text : string)
Section:CreateLabel("Revyn")


CreateButton(Button Text : string, Function : function)
Section:CreateButton("Print 'Hello World!'", function()
    print("Hello World!")
end)


CreateToggle(Toggle Text : string, Default State : boolean, Function : function)
Section:CreateToggle("Speed Walk", false, function(State)
    if State then
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 100
    else
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
    end
end)


CreateInput(Input Text : string, Placeholder Text : string, Function : function)
Section:CreateInput("Set Speed", "16", function(Input)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = tonumber(Input)
end)

CreateKeybind(Keybind Text : string, Default Keybind : Enum.KeyCode, Function :function)
Section:CreateKeybind("Change Keybind", Enum.KeyCode, function(NewKeybind)
    OpenMenuKeybind = NewKeybind
end)
